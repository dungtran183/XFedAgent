{
  "name": "xfedagent-artifacts",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "compile:linear-pov": "mkdir -p artifacts/circuits && circom circuits/binary_linear_pov.circom --r1cs --wasm --sym -o artifacts/circuits",
    "compile:contracts": "hardhat compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "circomlib": "^2.0.5",
    "hardhat": "^3.0.17",
    "snarkjs": "^0.7.5"
  }
}

